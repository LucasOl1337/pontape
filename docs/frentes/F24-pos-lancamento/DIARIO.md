# Diário · F24 · Ensaio pós-lançamento

## 23/09/2026 · Preparação

- Criei `bruto/f24-pos-lancamento` no meu worktree a partir de `origin/main` atualizado, após ler `AGENTS.md`, PRD, QUADRO, brief F24, checklist e ensaio F18.
- Escopo: conferir o site real `https://pontape.org` sem alterar Cloudflare ou `src/`; medir HTTPS, DNS, cabeçalhos, rotas, prévia, livro e carga; refazer visual da Crônica na bancada; revisar issues #40 a #43 e abrir issues para falhas novas. Entrega em `docs/operacao/ENSAIO-POS-LANCAMENTO.md` e PR.
- A F21b anterior foi encerrada por ordem do Regente sem PR; a Crônica escolhida já está na main. Seu ensaio preliminar havia encontrado transbordamento do livro com A+; vou reproduzir contra o site publicado antes de registrar como falha atual.

## 23/09/2026 · Primeira passada na produção

- DNS local ainda não resolve `pontape.org`; usei o IP `172.67.180.35` dado pelo Regente com `curl --resolve`, preservando Host e SNI. Criei a bancada exclusiva `pontape-f24-pos-lancamento` no workspace 6. O Chromium inicial não resolveu o domínio; reiniciei **somente o processo da minha bancada** com regra privada de resolução para `pontape.org` e `www.pontape.org`, sem mexer em DNS/flags globais. Confirmei `control_mode: agente`, workspace 6, `lives_in` e perfil da bancada antes de abrir uma aba própria da missão. A home real carregou.
- HTTPS da raiz retornou 200, TLS 1.3 com certificado válido para raiz e `www` (Google Trust Services, até 22/12/2026). `www` HTTPS também retorna 200 com conteúdo idêntico e canonical para a raiz. **HTTP raiz e www retornam 200 sem redirecionar para HTTPS**: falha de endurecimento a registrar após segunda conferência.
- Na resposta real, CSP com três hashes, `nosniff`, `Referrer-Policy`, `Permissions-Policy` e `X-Frame-Options` presentes. CSS em `/_astro/` tem `max-age=31536000, immutable`; fonte local tem `max-age=604800`; HTML/JSON `max-age=0, must-revalidate`. Cloudflare respondeu `CF-Cache-Status: HIT` nos exemplos consultados.
- Home/livro/404 responderam 200/200/404. `robots.txt` permite rastreamento; OG e Twitter usam imagens absolutas do domínio, e `og.png`/`og-transparencia.png` responderam 200. Baixei `/livro/ledger.json` para `/tmp/f24-ledger.json`: `python3 tools/conferir.py` informou **40 ações válidas**, saldo R$ 0,00. Próximo passo: medir interação, visual, carga e as issues no navegador real.

## 23/09/2026 · Régua de navegador e revisão das issues antigas

- No Chromium da bancada, home/livro/404 deram 200/200/404, com cabeçalho CSP, mas **uma violação de CSP em cada página**: Cloudflare injeta `static.cloudflareinsights.com/beacon.min.js`, bloqueado por `script-src 'self'` com hashes. Confirmei o `script` injetado no DOM. O único outro erro do console foi o 404 esperado na rota inexistente.
- Home 320 px: `scrollWidth` 305 sem A+ e 308 com A+; em 360 px, 345 nos dois modos. Fechei a [issue #42](https://github.com/LucasOl1337/pontape/issues/42) com comentário, pois o defeito da home v1 não vale no visual publicado. O livro **continua com problema novo**: 337/395 px em viewport 320 sem/com A+; com A+ 395 px em viewport 360. Zoom real 200% em janela física de 640 px deu viewport CSS 320 e livro 336 px. Será issue nova, não reabertura da #42.
- `prefers-reduced-motion` zerou transições/animações longas; primeira dobra em `forced-colors` legível. Contraste calculado do texto principal não achou AA abaixo do limite nas amostras sólidas (mínimo 5,17:1 home, 4,64:1 livro; seis amostras com gradiente não certificadas pixel a pixel). Tab percorreu 18/30 paradas home/livro; o degrau tem anel no bloco interno, mas o painel focável não tem anel próprio. Com `Input.dispatchKeyEvent` no Chromium isolado, setas percorreram os 9 degraus ida/volta; clique e link direto de todos os degraus abriram um painel no mesmo contêiner.
- Livro real no botão Conferir passou com 40 ações; exemplo fictício passou com 20; adulteração nº 2 detectada. A mensagem agora usa singular correto. Fechei a [issue #43](https://github.com/LucasOl1337/pontape/issues/43) com comentário. O livro vivo ganhou novas ações enquanto eu media (capturas posteriores mostraram 41 e depois 43), então a contagem final precisa ser reconferida ao fechar.
- Reproduzi a [issue #40](https://github.com/LucasOl1337/pontape/issues/40): na 404, “Como funciona” mantém a URL em `/nao-existe-f24#como-funciona`, sem elemento alvo; os outros três itens usam hashes locais equivalentes. Reproduzi a [issue #41](https://github.com/LucasOl1337/pontape/issues/41): exemplo marcado fictício, página 7/7, ação nº 1 “Decisão D999 registrada”, link “Ver a fonte” para commit de 40 zeros. Ambas ficam abertas.
- 3G lento contra Cloudflare HTTPS (400 ms RTT, 50 kB/s, cache de navegador desligado, 360×780): home `load` 3.649 ms / 136.808 B codificados; livro 4.265 ms / 164.373 B. Cloudflare respondeu comprimido (`content-encoding: zstd` em consulta equivalente). A falha de rede registrada é o beacon bloqueado pela CSP; os recursos próprios do site responderam 200.

## 23/09/2026 · Deploy durante o ensaio e passagem para a F26

- O Regente avisou da renomeação para `LucasOl1337/pontape`; mesclei `origin/main` em `bruto/f24-pos-lancamento`. O deploy automatizado entrou durante a medição. Repeti as respostas: HTTP raiz e www, e HTTPS www, agora dão 301 para HTTPS canônico; HSTS apareceu e os links públicos apontam ao repositório novo. A observação inicial de HTTP 200 é histórica, não uma falha atual.
- Repeti `npm run check` na base atual: lint, tipos, 127 testes, livro local, build, três hashes de CSP e orçamento passaram. Repeti 3G lento: home `load` 3.654 ms e livro 4.237 ms. Beacon externo da Cloudflare continua bloqueado pela CSP na home, livro e 404.
- Medi também a 404: em 320 px com A+, largura 353 px. O livro mantém 337/395 px em 320 sem/com A+ e 395 px em 360 com A+. Guardei prints em `docs/operacao/ensaio-pos-lancamento/`. No Chromium da bancada, botão Ouvir mostrou fallback de voz indisponível; módulo e caderno abriram e fecharam. Livro baixado às 03:41 UTC passou em `tools/conferir.py` com 45 ações; JSONs de eventos e checkpoint batem com o envelope.
- Abri [#54](https://github.com/LucasOl1337/pontape/issues/54) (404 A+), [#55](https://github.com/LucasOl1337/pontape/issues/55) (beacon/CSP), [#56](https://github.com/LucasOl1337/pontape/issues/56) (livro responsivo) e [#57](https://github.com/LucasOl1337/pontape/issues/57) (rollback de Workers). Relatório final em `docs/operacao/ENSAIO-POS-LANCAMENTO.md`.
- Ordem nova do Lucas: F26 vigília de polimento até 08:00 de Brasília, com executores baratos e sem mensagens ao Regente. A F24 vira o primeiro backlog em `docs/frentes/F26-vigilia-noturna/OPERACAO.md`. Despachei #56, #40, #41 e #54, um por executor. Próximo passo: PR documental e integração/revisão das PRs dos executores dentro da F26.
