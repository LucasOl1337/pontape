---
import type { IconName } from './icon-names';

interface Props { name: IconName; class?: string }
const { name, class: className = '' } = Astro.props;
---
<svg class:list={['icon', className]} aria-hidden="true" focusable="false"><use href={`#i-${name}`} /></svg>
